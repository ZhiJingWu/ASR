# 文本文件归一化换行符，检出时按 OS 转换（Windows CRLF / Linux macOS LF）
* text=auto eol=lf

# 明确指定某些文件为二进制，不参与换行转换
*.jar binary
*.war binary
*.class binary
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.ico binary
*.woff binary
*.woff2 binary
*.ttf binary
*.eot binary

# Windows 下脚本建议使用 CRLF（按需保留）
# *.bat text eol=crlf
# *.cmd text eol=crlf
# *.ps1 text eol=crlf
